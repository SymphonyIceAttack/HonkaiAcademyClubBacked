{
    "builds": [
        {
            "src": "./index.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.js"
        }
    ],
    "outputDirectory": ".",
    "installCommand": "pnpm install",
    "env": {
        "DATABASE_URL": "file:./dev.db"
    }
}